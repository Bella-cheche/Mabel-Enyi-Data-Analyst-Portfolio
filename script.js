const menu=document.querySelector('.menu'),links=document.querySelector('.links');menu?.addEventListener('click',()=>links.classList.toggle('open'));document.querySelectorAll('.links a').forEach(a=>a.addEventListener('click',()=>links.classList.remove('open')));function projectNotice(n){alert(n+' will be connected to its GitHub case study during the final link setup.')}function githubNotice(){alert('Your GitHub profile URL will be connected here during final setup.')}function emailNotice(){alert('Your professional email will be connected here during final setup.')}function resumeNotice(){alert('Your final CV will be connected to this button during final setup.')}